<%= form_for([@user, @news]) do |f| %>
	<% if @news.errors.any? %>
		<div id="error_explanation">
			<h2><%= pluralize(@news.errors.count, "error") %> prohibited this user from being saved:</h2>

			<ul>
				<% @news.errors.full_messages.each do |msg| %>
					<li><%= msg %></li>
				<% end %>
			</ul>
		</div>
	<% end %>

	<div class="field">
		<%= f.label "What's the latest?" %><br>
		<%= f.text_area :content %>
	</div>
	<div class="actions">
		<%= f.submit "Post" %>
	</div>
<% end %>